# Build directories
/cmake-build-*/
/out/
/dist/
build
/build-clangd/
/compile_commands.json
# macOS 适配：build_macos_installer.sh 默认使用 build/ 与 build_*/ 作为构建输出目录
/build/
/build_*/

# IDE configurations
/.vs/
/.idea/

# Temporary files
/_filterstate.*
/color.txt

# Test directories
/pngtest/

# Large binary files
/grid_plugin.zip

# OS-specific files
/desktop.ini

# Python cache files
__pycache__/
*.pyc

# CMake build artifacts
CMakeCache.txt
cmake_install.cmake
CMakeFiles/
Makefile
*.cmake

# Compiled objects and libraries
*.o
*.a
*.so
*.dll
*.lib
*.exe

# Debug and release builds
Debug/
Release/
*.pdb
*.idb

# JUCE specific build artifacts
JUCE/
JUCEArtwork/

# Allow third_party/JUCE (local checkout, committed for cross-machine consistency)
!/third_party/JUCE/

# macOS specific
.DS_Store

# Windows specific
Thumbs.db

# Backup files
*~
*.bak
*.backup

.vscodenul

# Test tools (keep scripts, ignore downloaded binaries)
/test_tools/projectm_native/projectMSDL/
/test_tools/projectm_native/src/
/test_tools/projectm_native/install/
/test_tools/projectm_native/vcpkg/
/test_tools/*.zipnul

# Windows reserved device names (cannot be tracked by git)
nul
tools/milkdrop_tester/nul

# AI 协作过程中偶发生成的编译辅助脚本 / 日志，不入库
/build_output.txt
/build_skill_verify.bat
/build_temp.bat
/incr_build.bat

# 旧版单文件 skill 打包产物（已被 docs/skills/milkdrop3-dev-guard/ 目录版本替代）
/docs/skills/milkdrop3-dev-guard.md
/docs/skills/milkdrop3-dev-guard.zip

# milkdrop3 引擎运行时用户偏好（暗色/pin 状态等），属于运行时状态，不入库
/assets/milkdrop_presets/settings.ini

# 说明：third_party/milkdrop3/ 已作为一手源码整体纳入版本管理，与 third_party/JUCE/
# 保持一致的入库方式（删除内部 .git 后直接提交），后续修改直接就地进行 commit。
